import styled from "styled-components";

export const OxhainAccordion = styled.div`
    .MuiAccordion-root {
        background-color: transparent;
        box-shadow: none;
        .MuiButtonBase-root {
            padding: 0;
        }
        .MuiAccordionDetails-root {
            padding: 0;
        }
        .Mui-expanded {
            margin: 10px 0;
        }
        .MuiAccordionSummary-root {
            margin: 0 0;
        }
        .MuiSvgIcon-root {
            font-size: 2rem;
        }
    }
`;